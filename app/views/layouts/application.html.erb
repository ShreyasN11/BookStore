<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bookstore" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Bookstore">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-stone-50 min-h-screen font-sans antialiased text-stone-900">
    <!-- Navbar -->
    <nav class="bg-white/80 backdrop-blur-md sticky top-0 z-50 border-b border-stone-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
          
          <!-- Logo -->
          <%= link_to root_path, class: "flex items-center gap-2 group" do %>
            <div class="bg-indigo-600 p-1.5 rounded-lg group-hover:bg-indigo-700 transition">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5s3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
              </svg>
            </div>
            <span class="text-xl font-bold tracking-tight text-stone-900">EBookStore</span>
          <% end %>

          <!-- Links -->
          <div class="flex items-center space-x-5">
            <% if user_signed_in? %>
              <%= link_to "My Bookings", bookings_path, class: "text-sm font-medium text-stone-600 hover:text-indigo-600 transition" %>
              <%= link_to "Logout", destroy_user_session_path, 
                          data: { turbo_method: :delete }, 
                          class: "text-sm font-medium text-stone-500 hover:text-red-600 transition" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "text-sm font-medium text-stone-600 hover:text-indigo-600 transition" %>
              <%= link_to "Sign Up", new_user_registration_path, 
                          class: "bg-stone-900 text-white px-5 py-2.5 rounded-full text-sm font-medium hover:bg-stone-800 transition shadow-sm" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <div class="max-w-xl mx-auto mt-4 px-4">
      <% if notice %>
        <div class="p-4 mb-4 text-sm text-emerald-700 bg-emerald-50 border border-emerald-100 rounded-xl shadow-sm animate-fade-in-down">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="p-4 mb-4 text-sm text-red-700 bg-red-50 border border-red-100 rounded-xl shadow-sm">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
